# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (keep .env.example tracked)
.env
.env.*
!.env.example

# Lockfiles from other package managers (this project uses npm)
bun.lock
yarn.lock
pnpm-lock.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cursor_renderblock_restore.txt

# Supabase local CLI state (not part of the product)
supabase/.temp/

# Local reference / scratch docs (not part of the product)
.workbuddy/
openclaw-*.md

# Maintainer / agent scratch (not part of the product surface)
.agent-handoff/
.agents/
.claude/
Scratchpad/
tasks.md
loop.sh
CLAUDE.md
